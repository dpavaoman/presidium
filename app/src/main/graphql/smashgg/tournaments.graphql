query TournamentsByVideogame {
    tournaments(query: {
        perPage: 3
        page: 1
        sortBy: "startAt asc"
        filter: {
            past: false
            videogameIds: [
                1
            ]
        }
    }) {
        nodes {
            id
            name
            slug
        }
    }
}